#!/bin/bash
docker build -f build-av/Dockerfile out/binaries/ -t ayudantevirtual/botpress:$TRAVIS_TAG
echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
docker push ayudantevirtual/ayudante-virtual:$TRAVIS_TAG
